---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java

inputs:
- name: esi-archive
- name: esi-client
- name: swagger-codegen-blazescala

outputs:
- name: client-latest
- name: client-legacy
- name: client-dev

run:
  path: /bin/bash
  args: ["./esi-client/compile.sh"]
